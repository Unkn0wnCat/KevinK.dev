<div class="postCard">
    <div class="postCardImage" style="background-image: url('{{ postImage }}');"></div>
    <div class="postCardMeta">
        <span class="postCardTitle">{{ postTitle }}</span>
        <span class="postCardTeaser">{{ teaserText }}</span>
    </div>
    <div class="postCardCTA"><a href="{{ link }}" class="spf-link">{% trans from "blog" %}read more{% endtrans %}</a></div>
</div>